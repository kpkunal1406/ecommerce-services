server:
  port: 8184

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    org:
      springframework: ERROR

spring:
  application:
    name: catalog-service
  datasource:
    url: jdbc:postgresql://localhost:55000/ecommerce_catalog
    username: postgres
    password: postgrespw
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
